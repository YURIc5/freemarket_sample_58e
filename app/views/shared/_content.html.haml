.box__contents__content
  = link_to item_path(item),method: :get,class: "contentlink" do
    %figure.figure
      %figcaption
        %span
          = item.description
      .tag
        %span{"aria-label" => "Price"} ¥ #{item.price}
        = image_tag "#{item.pictures.last.name}", class: 'tag__image'